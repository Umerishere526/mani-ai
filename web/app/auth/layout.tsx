// ABOUTME: Root layout for the auth section (login, and future signup/password-reset pages).
// ABOUTME: Each page owns its own full-height shell; this layout is a plain passthrough.

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
