# Runtime dependencies only. Test and CLI tooling lives in requirements-dev.txt so the
# deployed image does not carry a test runner. Transitive packages are resolved by pip;
# pinning only what is imported keeps this readable.

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0

asyncpg==0.31.0
PyJWT[crypto]==2.14.0

# LangChain composes the model call and parses the structured reply. langchain-openai is an
# OpenAI-*protocol* client pointed at OpenRouter's base_url - not a second provider, not a
# second bill - and it depends on this same openai package rather than bringing its own.
# LangSmith arrives with langchain and stays off: it would receive conversation transcripts.
langchain==1.4.1
langchain-openai==1.6.2
openai==3.14.0
httpx==0.28.1

sentry-sdk==2.69.1
python-dotenv==1.2.3
PyYAML==6.0.3
