# Everything the runtime does not need. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0

# `fastapi dev` only. Production runs uvicorn directly.
fastapi-cli==0.0.32
